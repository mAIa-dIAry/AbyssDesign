/** Wewnętrzny odstęp panelu wykresu (px). */
export const ABYSS_CHART_INSET_PX = 12;

/** Wysokość rzędu etykiet osi X — wpływa na grid, DOM i plotHeight (px). */
export const ABYSS_CHART_AXIS_LABEL_HEIGHT_PX = 14;
