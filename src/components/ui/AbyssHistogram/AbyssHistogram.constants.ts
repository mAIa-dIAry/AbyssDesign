/** Wewnętrzny odstęp panelu histogramu (px). */
export const ABYSS_HISTOGRAM_INSET_PX = 12;

/** Wysokość rzędu etykiet osi X — wpływa na grid, DOM i plotHeight (px). */
export const ABYSS_HISTOGRAM_AXIS_LABEL_HEIGHT_PX = 14;
